<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Ewallet" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/lato</item>
        <item name="android:textColor">@color/white</item>
        <!-- Primary brand color. -->
        <item name="android:windowFullscreen">true</item>
        <item name="colorPrimary">@color/orange</item>
        <item name="android:background">#171822</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="buttonStyle">@style/roundedButtonStyle</item>
    </style>

    <style name="AvatarBackgroundTint">
        <item name="android:tint">@color/avatar_background_black</item>
    </style>

    <style name="TitleTextColor">
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="IconTint">
        <item name="android:tint">@color/gray</item>
    </style>

    <style name="PowerIconTint">
        <item name="android:tint">@color/white</item>
    </style>

    <style name="NavHeaderBackgroundColor">
        <item name="android:backgroundTint">@color/header_black</item>
    </style>

    <style name="ThemeBackgroundColor">
        <item name="android:background">@color/gray_black</item>
    </style>

    <style name="CustomCardView" parent="">
        <item name="cardBackgroundColor">@color/gray_black</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">0dp</item>
    </style>

    <style name="roundedButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/rounded_button</item>
    </style>
</resources>